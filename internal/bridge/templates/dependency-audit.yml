name: Dependency Audit
description: Scan project dependencies for known vulnerabilities
prompt: |
  Audit all project dependencies for known security vulnerabilities.
  Check against public vulnerability databases. Report findings as
  a markdown summary with severity levels and recommended updates.
timeout: 1800
